/** Keep in sync with client `src/lib/pet-image-limits.ts` for UX hints. */
export const PET_IMAGE_MAX_BYTES = 8 * 1024 * 1024;
export const PET_IMAGE_MAX_LABEL = `${PET_IMAGE_MAX_BYTES / (1024 * 1024)}MB`;
