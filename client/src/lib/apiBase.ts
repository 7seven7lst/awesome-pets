/** Versioned API prefix; must match server `app.use("/api/v1", ...)`. */
export const API_V1_PREFIX = "/api/v1";

/** Auth API prefix; must match server `app.use("/auth/v1", authRouter)`. */
export const AUTH_V1_PREFIX = "/auth/v1";
